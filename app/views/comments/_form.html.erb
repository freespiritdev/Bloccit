<% if current_user %>
  <h4>Add a comment:</h4>

 <%= form_for [@post, @post.comments.new], remote: true do |f| %>   

    <%= f.label :body %>
    <%= f.text_area :body %>

    <%= f.submit "Save" %>
  <% end %>
<% end %>